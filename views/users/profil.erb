<!DOCTYPE html>
<html>
<head>
  <title>User-Profile Page</title>
</head>
<body>
<img src="/google_new.jpg" width=400 heigth=70 />
<p> Hello, welcome to your profile !
</p>
<% if @secret %>
<p> Thank you for adding your application to our authentication service!<br /><br />Please, store this secret in your application to be sure that your clients have been authenticated by our service: <%= @secret %></p>
<% end %>
<p> Applications you use : </p>

<% if @apps && !@apps.empty? %>
<div class="list">
<ul>
<% @apps.each do |appli| %>
<li><%=appli.name%> (<%=appli.url%>)&nbsp;&nbsp;</li>
<% end %>
</ul>
</div>
<%else%>
<p>No application uses your account.</p>
<%end%>

<p> Applications you have added : </p>
<% if @devs && !@devs.empty? %>
<div class="list">
<ul>
<% @devs.each do |dev| %>
<li><%=dev.name%> (<%=dev.url%>)&nbsp;&nbsp;<form action="/applications/<%=dev.name%>/delete" method="post"><input type="submit" value="Delete Appli" /></form></li>
<% end %>
</ul>
</div>
<%else%>
<p>No application added for your account.</p>
<%end%>

<a href ="/applications/new"> Add an application </a> <br />
<a href ="/sessions/disconnect"> Disconnect </a>
</body>
</html>
